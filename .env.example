# Copy to .env and fill in values for local runs.
# Database (required for agent tools and memory checkpointer)
PGDATABASE_URL=postgresql://user:password@localhost:5432/your_db

# LLM API (use either OpenAI or any OpenAI-compatible endpoint)
OPENAI_API_KEY=sk-your-openai-api-key
# Optional: use a different base URL (e.g. Azure OpenAI, local proxy)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: backend directory (auto-detected when using backend/scripts/)
# PROJECT_ROOT=/path/to/data-analysis-agent/backend

# Optional: S3 storage (only if using S3 features)
# BUCKET_ENDPOINT_URL=
# STORAGE_TOKEN=
